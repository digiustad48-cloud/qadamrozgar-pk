name: QadamRozgar Agent
on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: python agent_main.py --once
      - run: |
          git config --global user.name 'QadamRozgar Bot'
          git config --global user.email 'bot@qadamrozgar.com'
          git add jobs_db.json jobs_db_1000.json
          git diff --staged --quiet || git commit -m "final: 1000 jobs Govt Pvt Gulf secured"
          git push
